-	script	storage	-1,{
	if(getcharid(2) > 0)
	menu "คลังปกติ",-,"คลัง Guild (" + $important_color$ + "ต้องมี Skill คลัง Guild" + $end_color$ + ")",L_GuildStorage;
	openstorage;
	end;
	
	L_GuildStorage:
	guildopenstorage();
	end;
	
OnInit:
	waitingroom "คลังเก็บของ",0;
	end;
}

prontera,163,174,3	duplicate(storage)	คลังเก็บของ	860
