prontera,151,185,5	script	จัดตั้ง Guild	564,{
	// Check is character already had a guild?
	if(getcharid(2) > 0){
		mes "คุณมี Guild อยู่แล้ว";
		close;
	}
	mes "ระบุชื่อ Guild";
	input .@s$;
	if(.@s$ != "" && .@s$ != " "){
		atcommand "@guild " + .@s$;
		close;
	}
	else{
		mes "ไม่สามารถตั้ง Guild ด้วยชื่อดังกล่าวได้";
		close;
	}
	
OnInit:
	waitingroom "จัดตั้ง Guild",0;
	end;
}
