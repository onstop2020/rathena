-	script	logout	-1,{
OnPCLogoutEvent:

	// Unexpected Error
	if(!playerattached())
	end;

	if(compare("prontera|prt_in|job4_mag|malaya|que_moon",strcharinfo(3)))
	end;

	if(playerType <= 0)
	warp "prontera",156,191;

	end;
}
