-	script	logout	-1,{
OnPCLogoutEvent:

	// Unexpected Error
	if(!playerattached())
	end;

	if(playerType == 1)
	lastHintMapName$ = "";

	if(strcharinfo(3) == "prontera"
			|| strcharinfo(3) == "prt_in"
			|| strcharinfo(3) == "job4_mag"
			|| strcharinfo(3) == "malaya"
			|| strcharinfo(3) == "que_moon")
	end;

	if(playerType <= 0)
	warp "prontera",156,191;

	end;
}
