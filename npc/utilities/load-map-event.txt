prontera	mapflag	loadevent

-	script	LoadMapEvent	-1,{
	end;
	
OnPCLoadMapEvent:
	if(strcharinfo(3) == "prontera"){
		RefreshDebuff();
		
		// Bug equipment auto unequip
		if((getiteminfo(getequipid(EQI_HAND_R),ITEMINFO_SUBTYPE) == 11)
				&& (getequipid(EQI_HAND_L) > 0)
				&& (getiteminfo(getequipid(EQI_HAND_L),ITEMINFO_SUBTYPE) != 11)){
			if(getequipid(EQI_HAND_R) > 0)
			unequip EQI_HAND_R;
			
			if(getequipid(EQI_HAND_L) > 0)
			unequip EQI_HAND_L;
		}
	}

	end;
}
