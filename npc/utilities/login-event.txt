-	script	LoginEvent	-1,{
	end;
	
OnPCLoginEvent:
	// Unexpected error
	setpcblock PCBLOCK_ALL,false;
	// Back to town when login
	if(isPassTutorial
			&& (strcharinfo(3) != "prontera"))
	addtimer 1000,strnpcinfo(3) + "::OnBackToTown";
	end;
	
OnBackToTown:
	warp "prontera",156,191;
	end;
}
