// Buy more zeny
-	script	killedMonsterChecker	-1,{
	mes "ข้ามีหน้าที่ ตรวจสอบผู้สังหาร Monster";
	next;
	menu "ตรวจสอบข้อมูลของข้าให้ที",-,"ข้าอยากรู้ว่าข้าต้องฆ่า Monster ที่ห้องไหนในตอนนี้",L_Hint;
	mes "อ่า สักครู่นะ";
	next;
	.@killedMonster = getarraysize(#killedMonster);
	mes "Monster ที่ต้องฆ่ามีทั้งหมด: " + callfunc("F_InsertComma",getarraysize($mrid)) + " ตัว";
	mes "Monster ที่เจ้าฆ่าไปแล้ว: " + callfunc("F_InsertComma",.@killedMonster) + " ตัว";
	next;
	if(getarraysize($mrid) <= .@killedMonster)
	{
		mes "เจ้าฆ่าครบหมดเลย! \nจำนวนการคูณ EXP พิเศษ ที่เจ้าได้รับคือ \nx" + callfunc("F_InsertComma",(.@killedMonster/10));
		mes "ว่าแต่ทำไม Monster มันถึงยังเกิดอยู่อีกล่ะเนี่ย!";
		close;
	}
	else{
		mes "จำนวนการคูณ EXP พิเศษ ที่เจ้าได้รับคือ \nx" + callfunc("F_InsertComma",(.@killedMonster/10));
		mes "ข้าหวังว่าสักวัน Monster เหล่านี้ จะหายไปจากโลกของเราเสียที";
	}
	close;

	L_Hint:
	mes "อือออ ฮึ ขอข้าดูข้อมูลที่ข้ามีก่อน";
	next;

	.@killedMonster = getarraysize(#killedMonster);
	
	if(getarraysize($mrid) <= .@killedMonster)
	{
		mes "โอ้ววว เจ้าฆ่าครบหมดเลย ข้าหวังว่าข้าจะมีรางวัลให้เจ้าจัง T_T";
		close;
	}
	
	freeloop(1);
	
	.@currentRoomSize = getarraysize($mr1id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr1id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 1 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr2id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr2id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 2 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr3id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr3id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 3 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr4id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr4id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 4 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr5id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr5id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 5 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr6id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr6id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 6 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr7id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr7id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 7 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr8id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr8id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 8 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr9id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr9id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 9 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr10id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr10id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 10 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr11id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr11id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 11 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr12id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr12id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 12 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr13id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr13id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 13 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr14id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr14id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 14 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr15id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr15id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 15 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr16id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr16id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 16 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr17id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr17id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 17 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr18id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr18id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 18 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr19id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr19id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม Level ชั้น 19 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($allMvp);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $allMvp[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ ชั้นรวม MvP ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr22id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr22id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม EXP ชั้น 1 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr23id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr23id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม EXP ชั้น 2 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr24id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr24id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม EXP ชั้น 3 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr25id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr25id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม EXP ชั้น 4 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr26id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr26id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม EXP ชั้น 5 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}
	
	.@currentRoomSize = getarraysize($mr27id);
	for(.@i = 0; .@i <= .@currentRoomSize; .@i++)
	{
		.@isFound = 0;
		for(.@j = 0; .@j <= .@killedMonster; .@j++){
			if(#killedMonster[.@j] == $mr27id[.@i]){
				.@isFound = 1;
				break;
			}
		}
		if(!.@isFound){
			mes "คุณยังฆ่า Monster ที่ รวมตาม EXP ชั้น 6 ไม่ครบ";
			freeloop(0);
			close;
			break;
		}
	}

	freeloop(0);
	
	close;
	
OnInit:
	waitingroom "เช็คคูณ EXP พิเศษ",0;
	end;
}

prontera,134,172,3	duplicate(killedMonsterChecker)	ผู้บันทึก Monster	615 
